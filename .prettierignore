# Lockfile is machine-generated by pnpm and reformatting it would cause
# needless merge conflicts with dependency PRs. Excluded so the format
# gate only covers hand-written sources.
pnpm-lock.yaml

# Test fixtures. The project's tsconfig.json already excludes these
# (`**/__fixtures__/**`), so the format gate follows the same convention:
# fixtures are input data for tests, not code we hand-format.
**/__fixtures__/**
tests/integration/fixtures/**

# Build output (ignored even though it shouldn't be committed; safety net).
dist/
node_modules/
