# Minimal test workflow to isolate the parse failure.

name: integration

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * 1'
  pull_request:

concurrency:
  group: integration-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  smoke:
    name: Smoke
    runs-on: ubuntu-latest
    steps:
      - run: echo "smoke"