# Diagnostic: bare reusable call with NO inputs.

name: integration

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * 1'
  pull_request:

concurrency:
  group: integration-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    name: Quality gates
    uses: 0451-software/reusable-workflows/.github/workflows/node-quality-gates.yml@v1